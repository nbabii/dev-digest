/** Constants for the Agent editor's Skills tab. */

/** Pointer must move this many px before a drag starts — keeps a plain click
    on the checkbox/name from being swallowed as a drag gesture. */
export const DRAG_ACTIVATION_DISTANCE = 4;
