import type { IconName } from "@devdigest/ui";

/** Every skill card uses the same icon — only its background/icon color
    (see lib/skill-type-style.ts) varies by type. */
export const SKILL_ICON: IconName = "Sparkles";
